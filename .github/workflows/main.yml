name: CI - Iris Classifier with Report

on:
  push:
    branches: ["main","master", "feature/ci", "feature/test-ci"]
  pull_request:
    branches: ["main", "feature/ci", "master", "feature/test-ci"]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  test_model:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Set up CML
      uses: iterative/setup-cml@v2

    - name: Run unit tests
      run: |
        python -m pytest -q > test_output.txt || echo "Tests failed"

    - name: Train model and generate metrics
      run: |
        python train.py --model-path model.joblib --metrics-path metrics.csv

    - name: Create and post CML report
      env:
        REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        echo "# 🧪 Iris Model Report" > report.md
        echo "## ✅ Test Output" >> report.md
        echo '```' >> report.md
        cat test_output.txt >> report.md
        echo '```' >> report.md

        echo "## 📊 Training Metrics (Accuracy & Loss per Epoch)" >> report.md
        echo '| Epoch | Accuracy | Loss |' >> report.md
        echo '|-------|----------|------|' >> report.md
        tail -n +2 metrics.csv | while IFS=',' read -r epoch acc loss; do
          printf '| %s | %.4f | %.4f |\n' "$epoch" "$acc" "$loss" >> report.md
        done

        cml comment create report.md

